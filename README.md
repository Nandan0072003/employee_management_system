Employee Management System (Console-Based CRUD Application)

A fully functional console-based Employee Management System built using pure Java, MySQL, JDBC, and layered architecture with DAO pattern.
Implements complete CRUD operations (Create, Read, Update, Delete) with proper separation of concerns and SQL injection-safe queries using PreparedStatement.
